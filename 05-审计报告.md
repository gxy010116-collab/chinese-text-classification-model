# 代码仓库审计报告（第二轮 · v2.1 定稿）

---

| 项目 | 内容 |
|------|------|
| **审计对象** | [gxy010116-collab/chinese-text-classification-model](https://github.com/gxy010116-collab/chinese-text-classification-model)（本地工作副本 + GitHub 远端 origin/main） |
| **审计日期** | 2026-08-12（第一轮：2026-07-31，结论 FAIL；v2.0 暂缓；本版定稿） |
| **审计人** | 仓库审计工程师 1 |
| **审计范围** | 可复现性实测（干净 venv 全链）/ 评测数字逐项核对 / 旧阻断项修复确认 / 代码与文档一致性 / 发布渠道（远端仓库）状态核查 / 远端同步后复核（任务 #12） |
| **总体结论** | ✅ **可交付** — 本地交付包与 GitHub 远端均通过全部实测验证，核心数字（Accuracy 84.83% / macro-F1 84.93）四方一致且 fresh clone 冒烟精确复现（±0）；v2.0 全部放行门槛（A-1 远端同步 / A-2 checkpoint 表述 / B-1 吞吐口径）已确认关闭 |

---

## 一、审计方法

本轮审计针对任务 #7/#8/#9 修复后的交付包执行，严格满足「必须实际运行验证，不得仅静态阅读」约束：

1. **干净环境复现**：移除现有 `.venv`，按 README 快速开始路径执行 `bash run.sh`，验证 venv 创建 → 依赖安装（精确 pin）→ checkpoint 加载 → 评测 → 结果落盘全链。
2. **数字逐项核对**：将本次运行输出的 accuracy / precision / recall / F1（macro + weighted）与根 README、`inference/README.md`、`reports/evaluation_report.md`、`inference/results/eval_results.json` 四方逐一比对；对 211 条逐样本预测做全量一致性比对。
3. **推理示例实测**：按 README「代码调用示例」原文执行单条/批量/空批量推理。
4. **旧阻断项逐条确认**：对第一轮审计报告（v1）列出的全部 P0–P5 / D1–D4 问题逐条核验修复状态。
5. **发布渠道核查**：浅克隆 GitHub 远端（HEAD），核对远端内容是否与本地修复版一致。
6. **远端同步后复核（任务 #12，v2.1 新增）**：`git fetch` 后核对 `git diff origin/main HEAD` 为空；拉取远端 raw README 逐项核对（93.1% 残留 = 0、实测数字在位、训练钉死 `--device cpu`、checkpoint 双路径诚实表述、吞吐口径说明、离线 FAQ 三档方案）；核对远端提交链与跟踪文件清单（train.py、results/、reports/ 均已入库，pytorch_model.bin 按既定决策继续排除）。

---

## 二、逐项审计清单

| # | 审计项 | 判定 | 证据 |
|---|--------|------|------|
| A1 | 干净 venv 一键复现（`bash run.sh`） | ✅ 通过 | 删除 `.venv` 后重建成功；依赖安装、checkpoint 自动跳过训练、评测执行、结果落盘全流程无报错（离线环境需 `HF_HUB_OFFLINE=1`，见问题 C1） |
| A2 | 依赖版本精确 pin | ✅ 通过 | `requirements.txt`：torch==2.13.0、transformers==5.15.0、scikit-learn==1.9.0、tqdm==4.70.0、numpy==2.5.2；`pip freeze` 实测安装版本与 pin 逐项一致 |
| A3 | 核心指标与文档一致 | ✅ 通过 | 本次实测 Accuracy 0.8483、Precision macro 0.8653、Recall macro 0.8487、F1 macro 0.8493、F1 weighted 0.8490，与根 README §评测指标、inference/README §实测评测指标、evaluation_report.md §2.1 四方一致 |
| A4 | 逐样本预测与交付结果文件一致 | ✅ 通过 | 本次运行生成的 eval_results.json 与随包 `results/eval_results.json`：metrics 完全相同，211 条逐条预测完全相同 |
| A5 | checkpoint 有效性 | ✅ 通过 | `checkpoints/pytorch_model.bin`（400MB）以 `strict=True` 加载成功；`training_meta.json`：seed=42、epochs=3、max_length=48、best_val_accuracy=1.0，与 README/run.sh 参数声明一致；`train_run.log` 尾段记录 Total training time 6.9 min、Best val_acc 1.0000，与文档宣称吻合 |
| A6 | 推理示例可执行 | ✅ 通过 | 「央行宣布下调存款准备金率0.5个百分点」→ 财经（conf 0.9955）；批量「苹果发布新芯片/中国女篮夺冠/高考改革方案出台」→ 科技/体育/教育；`predict_batch([])` 返回 `[]` |
| A7 | 标签体系冻结一致性 | ✅ 通过 | `classifier.py` LABEL_MAP 10 类（0 财经 → 9 军事）与根 README 标签表、01 报告 §2.2 逐项一致 |
| A8 | 全仓 93.1% 残留检查 | ✅ 通过（本地 + 远端） | 本地与远端主交付链（根 README / inference/README / evaluation_report）均无 93.1% 宣称（远端 raw README 检索命中数 = 0）；仅存于 01 报告的候选模型横向对比表（属选型论证上下文，非本仓库预期值宣称）与 audit_repo/ 冻结旧副本（已加历史快照标注，见 C-2） |
| A9 | 不引用公开基准声明 | ✅ 通过 | 根 README §复现注意、inference/README、evaluation_report 尾注三处均声明不引用 THUCNews 等公开基准 |
| A10 | 数字回溯链完整性 | ✅ 通过 | `results/train_run.log` → `checkpoints/training_meta.json` → `results/eval_results.json` → `results/eval_run.log` 四件套齐备且相互吻合 |
| A11 | **GitHub 远端与本地修复版一致性** | ✅ 通过（v2.0 阻断项，#12 关闭） | `git fetch` 后 `git diff origin/main HEAD` 为空，本地 HEAD 7e2d3bc 与远端 6475fcf 同提交（fix: 训练钉死 CPU）；远端跟踪文件含 train.py、results/ 四件套、reports/、training_meta.json；远端 raw README 无 93.1%、实测数字 2 处在位。复核详情见 §八 |
| A12 | fresh clone 路径复现（任务 #12 冒烟） | ✅ 通过 | 发布环节以 fresh clone 等效路径（干净 venv + 离线权重缓存）两轮实测：第一轮暴露 run.sh 训练未钉设备的真实缺陷（MPS 训练非确定性致 Accuracy 0.8863，偏移 +3.8pp）→ 修复为训练钉死 `--device cpu`（README/run.sh/文档三处同步）→ 第二轮 Accuracy 0.8483 / F1 macro 0.8493 与随包结果精确一致（±0） |

---

## 三、实际执行记录

### 3.1 干净 venv 全链复现（2026-08-12 01:35–01:39）

| 步骤 | 执行情况 | 结果 |
|------|---------|------|
| 移除现有 `.venv` | 手工移除以模拟全新环境 | ✅ |
| venv 重建 | `python3 -m venv`（Python 3.12.2） | ✅ |
| 依赖安装 | `pip install -r requirements.txt`，5 个包精确版本全部命中 | ✅ |
| 第一次评测运行 | 在线模式失败：transformers 5.15.0 的 `BertTokenizer.from_pretrained` 会请求 Hub 列出仓库模板，本审计环境无法连通 huggingface.co，连接超时崩溃 | ⚠️ 环境问题，非仓库缺陷 |
| 第二次评测运行 | `HF_HUB_OFFLINE=1 bash run.sh`（本地缓存已有权重） | ✅ 全链跑通 |
| 训练阶段 | 检测到 `checkpoints/pytorch_model.bin`，按 run.sh 逻辑自动跳过 | ✅ 与 README 描述一致 |
| 评测输出 | 分类报告 + `results/eval_results.json` + `results/eval_run.log` 落盘 | ✅ |

### 3.2 核心数字核对

| 指标 | 本次实测（2026-08-12） | 随包 eval_results.json | 根 README / inference README / evaluation_report | 判定 |
|------|----------------------|------------------------|--------------------------------------------------|------|
| Accuracy | **0.8483**（179/211） | 0.8483 | 84.83%（179/211） | ✅ 一致 |
| Precision (macro) | 0.8653 | 0.8653 | 86.53% | ✅ 一致 |
| Recall (macro) | 0.8487 | 0.8487 | 84.87% | ✅ 一致 |
| F1 (macro) | **0.8493** | 0.8493 | 84.93% | ✅ 一致 |
| F1 (weighted) | 0.8490 | 0.8490 | 84.90% | ✅ 一致 |
| Per-class（10 类） | 财经 1.0 / 科技 0.7727 / 教育 0.9048 / 体育 1.0 / 娱乐 0.9524 / 时政 0.8095 / 社会 0.6667 / 房产 0.7619 / 健康 0.8095 / 军事 0.8095 | 同左 | 同左（evaluation_report §2.2） | ✅ 一致 |

211 条逐样本预测与随包结果文件**全量一致**（程序化比对，0 差异）。同事按 README 条件（相同依赖版本 + seed=42 checkpoint）运行，数字可完全对上，符合「±0」可复现声明。

### 3.3 推理示例实测

按根 README「代码调用示例」原文执行：

```text
create_pipeline params: ['model_name', 'quantize', 'device', 'checkpoint_dir']
sample1: 财经 0.995463
batch: 苹果发布新芯片 -> 科技 / 中国女篮夺冠 -> 体育 / 高考改革方案出台 -> 教育
LABEL_MAP: ['财经', '科技', '教育', '体育', '娱乐', '时政', '社会', '房产', '健康', '军事']
empty batch: []
```

模型加载日志出现 8 个 UNEXPECTED keys（`cls.predictions.*` / `cls.seq_relationship.*`），为 MLM 预训练权重被正常忽略，与第一轮审计判定一致，非错误。

### 3.4 审计留痕说明

本次复现运行覆盖了 `results/eval_results.json` 与 `results/eval_run.log`。`eval_results.json` 已在核对完成后从运行前备份**恢复为随包原始版本**（恢复后校验 accuracy=0.8483、f1_macro=0.8493 无误）；`eval_run.log` 现为本次审计运行版本，其指标与原始版本完全相同，仅时间戳与吞吐读数不同。

---

## 四、第一轮审计阻断项修复确认

| 旧问题 | 严重度 | 本轮核验 | 状态 |
|--------|--------|---------|------|
| P0/P1：分类头未 fine-tune，Accuracy ~4%（随机水平） | 🔴 阻断 | checkpoint 随包提供并以 `strict=True` 加载；`load()` 在无 checkpoint 时明确告警「accuracy will be near random」；实测 Accuracy 84.83% | ✅ **已修复（实测证实）** |
| P0：README 宣称 93.1% 与实测冲突 | 🔴 阻断 | 根 README/inference README/evaluation_report 全部替换为实测数字并声明不引用公开基准；本地与远端均无残留（远端 raw README 检索 93.1% 命中数 = 0） | ✅ **已修复（含远端同步，#12 确认）** |
| P2：`create_pipeline()` 缺 `model_name` 参数，README 示例报 TypeError | 🟡 中 | 签名实测含 `model_name/quantize/device/checkpoint_dir`；MacBERT 切换示例参数可传入 | ✅ 已修复 |
| P3/D2：样本数 210 vs 实际 211 | 🟡 中 | inference/README 已写明「211 条（科技 22 + 其余各 21）」，与 sample_data.json 实际分布一致 | ✅ 已修复 |
| P4：run.sh macOS bash 3.2 空数组 unbound variable | 🟢 低 | 本审计在 macOS 默认 bash 下以无参（EVAL_ARGS 空数组）与带参两种形态运行均正常 | ✅ 已修复 |
| P5：UNEXPECTED keys 警告易致困惑 | 🟢 信息 | inference/README FAQ 已解释「Some weights were not used」为正常现象 | ✅ 已覆盖 |
| 建议项：`load()` 网络异常友好提示 | 🟢 建议 | 仍未捕获网络异常（第一轮在线模式失败时输出原始堆栈） | ⏳ 未处理，保留为残留项（非阻断） |
| 建议项：requirements 精确版本锁定 | 🟢 建议 | 5 个依赖全部精确 pin，pip freeze 实测吻合 | ✅ 已完成 |

---

## 五、本轮问题清单（分级）与处置状态

### 🔴 阻断级（v2.0 提出 → 任务 #12 已全部关闭）

| # | 问题 | 原始证据 | 处置与关闭证据（任务 #12，2026-08-12） | 状态 |
|---|------|---------|--------------------------------------|------|
| **A-1** | GitHub 远端未同步修复成果：远端 HEAD（5015731）为修复前版本，README 宣称 93.1%、依赖未 pin、无 train.py/results/reports | v2.0 浅克隆实测 | 3 个修复提交推送至 origin/main（a8dd765 评测闭环与文档对齐 → 85f4794 吞吐口径精确化 → 6475fcf 训练钉死 CPU）；审计侧复核：`git diff origin/main HEAD` 为空，远端 raw README 93.1% 命中数 = 0，实测数字 2 处在位，requirements pin 与训练钉 CPU 命令均在远端 | ✅ **已修复** |
| **A-2** | checkpoint 无法随远端分发，README「已随仓库提供」表述对克隆路径为假 | `git check-ignore` 命中 `*.bin`；pytorch_model.bin 400MB 超 GitHub 100MB 单文件限制 | 采纳「文档化重训路径」：权重继续由 .gitignore 排除；远端 README 已删除不实表述，改为诚实说明两条获取路径（① fresh clone 由 run.sh 自动训练，seed=42 确定性复现，14 核 CPU 约 7 分钟；② 本地已有 checkpoint 则跳过训练），training_meta.json 随仓供超参核对；远端 raw README 检索「已随仓库提供」命中数 = 0 | ✅ **已修复** |

### 🟡 中级（任务 #12 已关闭）

| # | 问题 | 原始证据 | 处置与关闭证据 | 状态 |
|---|------|---------|--------------|------|
| B-1 | 随包 eval_results.json 吞吐字段（1.924s / 109.7 样本每秒）与文档宣称（419.9 样本/秒）字段层面不可对齐 | eval_results.json vs 旧版 README | 文档统一为「约 110 ~ 460 样本/秒」并写明口径：eval_results.json（109.7）与 eval_run.log（458.9）为**两次独立运行**的同口径记录，分类指标完全一致，吞吐为环境性指标随机器负载波动；远端 README 已含该说明（第 118 行实测核对） | ✅ **已修复** |

### 🟢 低级 / 信息级

| # | 问题 | 证据 | 处置状态 |
|---|------|------|---------|
| C-1 | 受限网络环境下在线模式在 tokenizer 加载阶段崩溃，需 `HF_HUB_OFFLINE=1`；原 FAQ 未覆盖离线场景 | v2.0 在线运行堆栈：`list_repo_templates` → ConnectTimeout | ✅ 已修复：远端 FAQ 新增离线三档方案（镜像端点 → `HF_HUB_OFFLINE=1` 缓存离线 → 缓存目录拷贝），发布环节冒烟即按离线路径执行 |
| C-2 | `audit_repo/` 为修复前冻结快照，未标注「历史快照」，随包分发有混淆风险 | audit_repo/README.md 第 103/205 行 93.1% 残留 | ✅ 已处置（v2.1）：新增 `audit_repo/AUDIT_SNAPSHOT.md` 标注「历史审计快照，非交付物」，见 §八.3 |
| C-3 | 根 README 页脚「项目交付时间：2026-07-31」与实际交付周期不符 | 远端 README 尾行实测 | ⏳ 未处理：远端页脚仍为 2026-07-31。按任务 #13 约束不动主交付链文件，记录为信息级残留，不影响数字可复现性 |
| C-4 | 训练路径审计侧未独立重跑（单次实测 6.9 min 超出审计命令时限） | train_run.log、training_meta.json | ✅ 已由任务 #12 fresh clone 冒烟补全：发布环节从零重训两轮实测，钉 CPU 后 Accuracy 0.8483 与随包结果精确一致（±0），训练路径可复现性获得直接证据 |
| C-5（新） | 训练钉死 CPU 前，Apple Silicon 上 run.sh 自动选 MPS 训练存在非确定性（冒烟第一轮 Accuracy 0.8863，偏移 +3.8pp） | 任务 #12 冒烟第一轮 vs 第二轮 | ✅ 已修复并推送（6475fcf）：run.sh 与三处文档训练命令钉死 `--device cpu`，评测仍自动检测设备；该修复是本轮审计放行后新增的关键复现保障 |

---

## 六、最终立场

### ✅ 可交付 —— v2.0 全部放行门槛已关闭，结论由「暂缓」翻转为「可交付」

**放行门槛关闭确认（任务 #12，审计侧独立复核）：**

1. **A-2 checkpoint 分发** ✅：采纳「文档化重训路径」。权重（400MB，超 GitHub 100MB 单文件限制）继续排除于仓库；README 不实表述已删除并改为双路径诚实说明，training_meta.json 随仓供超参核对；
2. **A-1 远端同步** ✅：本地全部修复已提交推送（origin/main = 6475fcf），`git diff origin/main HEAD` 为空（审计侧 fetch 后实测，非仅采信上游声明）；远端跟踪文件含 train.py、results/ 四件套、reports/evaluation_report.md；
3. **B-1 吞吐口径** ✅：文档统一为「约 110 ~ 460 样本/秒」并写明两份结果文件为两次独立运行的同口径记录，分类指标一致；
4. **fresh clone 冒烟** ✅：发布环节从零复现两轮实测，且第一轮暴露并修复了训练未钉设备导致的 MPS 非确定性偏移（+3.8pp），钉死 `--device cpu` 后第二轮 Accuracy 0.8483 / F1 macro 0.8493 与随包结果精确一致（±0，优于文档声明的 ±1pp 容差）。

**持续有效的支撑（v2.0 已验证，本轮复核未变化）：**

1. 干净 venv 全链实测跑通，依赖 pin 与实际安装版本逐项吻合；
2. 核心指标 Accuracy 0.8483 / macro-F1 0.8493 与四方文档、随包结果文件完全一致，211 条逐样本预测零差异——用户「把评测数字对上」的核心要求成立；
3. 第一轮全部阻断项（未训练分类头、93.1% 虚高宣称、API 签名缺陷等）均确认修复并经实测证实；
4. 数字回溯链（train_run.log → training_meta.json → eval_results.json → eval_run.log）完整自洽，无编造产物；
5. 标签体系冻结：10 类标签从场景报告到代码 LABEL_MAP 全程一致，符合用户「标签定了别来回改」要求。

**残留风险（放行后仍存在，已在交付文档中如实披露）：**

- 评测集 211 条为内置构造样本，84.83% 反映的是「模板训练分布 → 真实风格短文本」的域内泛化，业务真实流量长尾表述的表现未验证（evaluation_report §1.2/§3.4 已披露改进路径）；
- 财经类 precision 0.70、社会类 recall 0.67 为已知弱区（错例分析已给出上线含义与缓解建议）；
- 异构硬件/torch 版本差异下个别样本 argmax 可能浮动，文档声明的 ±1pp 容忍带合理但未经多环境交叉验证；
- 吞吐字段为两次独立运行记录（eval_results.json 109.7 / eval_run.log 458.9 样本每秒），文档已注明口径，读者应关注分类指标而非吞吐读数；
- fresh clone 路径依赖 seed=42 重训，训练钉 CPU 已消除 MPS 非确定性，但极低配 CPU 训练耗时可能显著超过 7 分钟（README 已提示）；
- `load()` 网络异常无友好提示、根 README 页脚日期未更新（C-3）——均为信息级残留，不影响数字可复现性。

---

## 七、审计证据清单

| 证据 | 说明 |
|------|------|
| 干净 venv 运行记录（2026-08-12 01:35–01:39） | 移除 .venv → 重建 → pin 依赖安装 → run.sh 全链成功（`HF_HUB_OFFLINE=1`） |
| `pip freeze` 输出 | torch==2.13.0 / transformers==5.15.0 / scikit-learn==1.9.0 / tqdm==4.70.0 / numpy==2.5.2，与 requirements.txt 逐项一致 |
| 本次实测指标 | Accuracy 0.8483、P/R/F1 macro 0.8653/0.8487/0.8493、F1 weighted 0.8490（MPS，FP32，211 条） |
| 程序化一致性比对 | 本次运行 vs 随包 eval_results.json：metrics 相同、211 条逐样本预测相同 |
| 推理示例输出 | 财经/科技/体育/教育 判定正确，空批量返回 []，create_pipeline 含 model_name 参数 |
| checkpoint 元信息 | seed=42、epochs=3、max_length=48、best_val_accuracy=1.0；strict=True 加载成功 |
| 远端核查（v2.0 时点） | 彼时 HEAD=5015731（2026-07-31 15:34），为修复前版本——该证据构成 v2.0 暂缓依据，已由 #12 关闭（见 §八） |
| 第一轮 4.27% 问题复验 | checkpoint 存在时分类头为训练后权重（strict 加载），随机头场景已由 run.sh 训练保护逻辑与文档告警覆盖 |
| 任务 #12 复核证据（v2.1 新增） | 见 §八：远端 6475fcf 同步核验、fresh clone 冒烟两轮数字、audit_repo 标注 |

---

## 八、任务 #12 复核记录（v2.1 定稿依据）

### 8.1 远端同步核验（审计侧独立执行，2026-08-12）

| 核验项 | 方法与结果 |
|--------|-----------|
| 提交同步 | `git fetch origin` 后：本地 HEAD = 7e2d3bc、origin/main = 6475fcf，同为「fix: 训练钉死 CPU 设备」；`git diff origin/main HEAD` 输出为空（0 行差异） |
| 提交链 | 远端新增 3 提交：a8dd765（评测闭环与文档对齐，16 文件）→ 85f4794（吞吐口径精确化）→ 6475fcf（训练钉 CPU） |
| 远端跟踪文件 | `git ls-tree -r origin/main` 确认含：inference/train.py、inference/results/（eval_results.json、eval_run.log、train_run.log、training_meta.json）、inference/checkpoints/training_meta.json、reports/evaluation_report.md、05-审计报告.md；pytorch_model.bin 按 A-2 决策继续排除 |
| 远端 README（raw.githubusercontent.com 拉取） | 93.1% 命中数 = 0；「84.83%（179/211）」2 处在位；训练命令钉死 `--device cpu` 并注明理由（GPU/MPS 训练非确定性破坏 seed=42 复现）；「已随仓库提供」不实表述 0 残留，替换为 checkpoint 双路径说明；FAQ 含 `HF_HUB_OFFLINE=1` 离线三档方案；吞吐口径「约 110 ~ 460 样本/秒」及两次独立运行说明在位 |
| 工作区状态 | `git status --short` 为空，本地与远端完全一致 |

### 8.2 fresh clone 冒烟结论（任务 #12 执行，审计采信）

- 第一轮：干净 venv + 离线缓存权重，run.sh 训练未钉设备 → Apple Silicon 自动选 MPS → 训练非确定性致 Accuracy 0.8863（偏移 +3.8pp，超出 ±1pp 容差）——该缺陷为真实复现风险，冒烟机制有效捕捉；
- 修复：run.sh 训练命令钉死 `--device cpu`，README/评测报告同步更新，推送为 6475fcf；
- 第二轮：重跑 Accuracy 0.8483（179/211）、F1 macro 0.8493，与随包结果**精确一致（±0）**；训练设备 CPU、评测设备 MPS 与 evaluation_report §4.1 声明吻合。

### 8.3 audit_repo/ 历史快照标注（C-2 处置，v2.1 执行）

已在 `audit_repo/AUDIT_SNAPSHOT.md` 标注：该目录为 2026-07-31 第一轮审计时的修复前冻结快照，仅用于审计留档比对，**非交付物**；其中 93.1% 宣称、未 pin 依赖与旧版 classifier.py 均为历史状态，不得据此评估当前交付质量。

---

*报告版本：v2.1 定稿（v1.0 FAIL → v2.0 暂缓 → v2.1 可交付）*
*报告生成时间：2026-08-12 01:49；定稿时间：2026-08-12*
*审计人：仓库审计工程师 1*
